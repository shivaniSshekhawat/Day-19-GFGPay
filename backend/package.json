{
  "name": "cinebook-movie-ticketing-system",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "npm install --prefix ../frontend && npm run build --prefix ../frontend && rm -rf dist && cp -r ../frontend/dist ./dist"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.4",
    "redis": "^5.10.0",
    "stripe": "^20.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
